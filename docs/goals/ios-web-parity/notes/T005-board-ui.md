# T005 Worker Receipt: Native Board UI

## Result

`done`

## Summary

Implemented the native iOS Board tab on top of the verified workbench contract. The app now has a first-class cross-repo issue board with repo filtering, status filtering, summary counters, running-session context, and navigation into existing issue detail flows.

## Implementation Notes

- Added `WorkbenchStore` as the Board's observable data owner. It fetches `/api/v1/workbench`, maintains selected repos and filters, flattens repo issue summaries, matches issue deployments by `targetType == .issue`, and keeps running issues sorted ahead of other open work.
- Added `BoardView` with a native iOS layout instead of copying the desktop shell. It includes refresh, filter, settings, repo-scope controls, open/running/closed filters, summary tiles, and issue cards with stable UI test identifiers.
- Added the Board tab between Today and Issues, matching the intended Today, Board, Issues, PRs, Active tab shape from the goal charter.
- Expanded the UI mock workbench payload to include two repos and issue summaries so tests can prove the board is truly cross-repo.
- Split broad toolbar UI coverage into smaller tests and fixed tab-selection/modal-dismiss helpers so the fifth tab and restored modals do not make unrelated toolbar checks flaky.
- `apple/IssueCTL/Generated/AppVersion.swift` was regenerated by iOS test/build tooling again; the generated diff was intentionally reverted because it is outside the slice.

## Verification

- Red check: `IssueCTLUITests/IssueCTLUITests/testBoardTabShowsCrossRepoIssueQueueAndRunningFilter` failed before implementation with missing Board tab/label, as expected.
- `git diff --check` passed.
- `IssueCTLUITests/IssueCTLUITests/testBoardTabShowsCrossRepoIssueQueueAndRunningFilter` passed after implementation.
- `IssueCTLUITests/IssueCTLUITests/testBoardToolbarActionsAreReachable` passed.
- `IssueCTLUITests/IssueCTLUITests/testIssueToolbarActionsAreReachable` passed.
- `IssueCTLUITests/IssueCTLUITests/testPullRequestToolbarActionsAreReachable` passed.
- `IssueCTLUITests/IssueCTLUITests/testActiveSessionToolbarActionsAreReachable` passed after the tab helper fix.
- `IssueCTLUITests/IssueCTLUITests/testListToolbarActionsAreReachableFromTabs` passed after the restored-modal helper fix.
- Focused unit/model/API suites passed: `IssueCTLTests/ViewLogicTests`, `IssueCTLTests/APIClientTests`, `IssueCTLTests/APIClientExtensionTests`, and `IssueCTLTests/ModelDecodingTests` ran 177 tests with 0 failures.

## Remaining Risk

The full `IssueCTL` scheme did not complete locally during this slice: the MCP full-scheme run hit the 120s timeout, and a raw `xcodebuild test` attempt failed with CoreSimulator service connection errors. Focused unit/API/model and Board/UI coverage passed, but final PR readiness should include a fresh full-scheme run or CI confirmation after the simulator service is stable.

Automation settings, webhook health controls, and label-control UX remain queued. Active session parity is partly covered by T003 and the toolbar checks, but T006 should audit and finish the remaining target-aware session behavior before moving to settings.

## Next Task

Activate `T006` to complete the active-session parity audit and any remaining issue/PR session UI or request-body fixes.
