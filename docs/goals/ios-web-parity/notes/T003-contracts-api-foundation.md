# T003 Worker Receipt: Contracts/API Foundation

## Result

`done`

## Summary

Implemented the first iOS/web parity foundation slice in the fresh main-based checkout at `c309bef6f127d1326810f5d428fd3f28b988c48c`.

The slice adds target-aware deployment decoding, repo automation fields, workbench payload decoding, workbench caching, webhook health/configuration calls, repo label recreation, PR label toggling, mock-server support, and the two mobile REST shims needed for webhook health and PR labels. It also ports the minimal active-session safety work so PR sessions no longer decode or navigate as issue sessions.

## Implementation Notes

- Kept workbench Swift contracts in `Repo.swift` instead of a new `Workbench.swift` file because the Xcode project does not auto-include new Swift files and T003 should avoid project regeneration churn.
- `ActiveDeployment.issueNumber` remains as a compatibility convenience, but PR sessions decode through `targetType == .pr` and `targetNumber`; all touched issue matching now gates on `targetType == .issue`.
- Workbench fixtures now use the web app's actual camelCase contract shape while retaining decoder compatibility with older snake_case API responses.
- Workbench cache invalidation now runs after both issue and PR label mutations, so automation-label changes can be reflected by the upcoming Board surface.
- `apple/IssueCTL/Generated/AppVersion.swift` was regenerated by `xcodebuild`; the generated diff was intentionally reverted because it is outside the slice.

## Verification

- `git diff --check` passed.
- `/Users/neonwatty/Desktop/issuectl/node_modules/.bin/vitest run labels workbench 'app/api/v1/repos/[owner]/[repo]/webhook/health/route.test.ts' 'app/api/v1/pulls/[owner]/[repo]/[number]/labels/route.test.ts'` passed: 6 files, 27 tests.
- `/Users/neonwatty/Desktop/issuectl/node_modules/.bin/tsc --noEmit` passed from `packages/web`.
- `xcodebuild test -project apple/IssueCTL.xcodeproj -scheme IssueCTL -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:IssueCTLTests/EnumTests -only-testing:IssueCTLTests/ModelDecodingTests -only-testing:IssueCTLTests/APIClientTests -only-testing:IssueCTLTests/APIClientExtensionTests` passed: 140 tests.
- Bundled `pnpm --dir packages/web test -- webhook|labels|workbench` and `pnpm --dir packages/web typecheck` could not run directly in the fresh checkout because local `vitest`/`tsc` binaries are absent after `pnpm install --offline` failed on a missing offline tarball. The focused web checks above used the already-installed toolchain from the original checkout against this fresh checkout's source.
- The broad borrowed-toolchain `vitest run webhook` pattern still fails before exercising code in DB-backed webhook tests because `better-sqlite3.node` in the original checkout was compiled for a different Node module ABI.

## Remaining Risk

This is only the contracts/API foundation. The iOS Board tab, WorkbenchStore, automation settings UI, and label-control UI remain queued. The dependency-cache issue should be cleaned up before a final PR-ready verification pass, but it did not block focused source verification for this slice.

## Next Task

Activate `T004` Judge to review this foundation before Board UI work begins.
