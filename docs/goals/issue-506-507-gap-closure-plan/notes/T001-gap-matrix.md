# T001 Gap Matrix

## Current-State Caveat

The board intake said the current branch was `codex/issue-506-507-gap-fixes`, but `git status --short --branch` reported `## main...origin/main` with only this GoalBuddy board untracked. The matrix below reflects current `main`, which the goal instructions identify as authoritative current state.

## Deduplicated Matrix

| Priority | Source | Finding | Classification | Evidence | Proposed fix slice | Verification |
|---|---|---|---|---|---|---|
| P0 | #506 | PR review launch transition is not recovery-safe between `launching`, deployment activation, and `in_progress`; crash can leave an active deployment without linked review or a stuck review lock. | True gap | [webhook-pr-intent.ts](/Users/neonwatty/Desktop/issuectl/packages/web/lib/webhook-pr-intent.ts:105) marks review launching, calls launch, then links deployment at line 110. | Add recovery-safe PR launch bookkeeping and startup repair for `reserved`/`launching`/`in_progress` rows whose deployment is missing, ended, or activated-but-unlinked. | `pnpm --dir packages/web test -- webhook-pr-intent webhook-pr-controls`; `pnpm --dir packages/core test -- pr-reviews deployments` |
| P0 | #506 | Startup recovery does not visibly handle orphaned `pr_reviews.reserved` / `launching` or `in_progress` with missing/ended deployment. | True gap | [webhook-intent-worker.ts](/Users/neonwatty/Desktop/issuectl/packages/web/lib/webhook-intent-worker.ts:72) recovers webhook intent leases and expires intents; no PR review recovery function appears in that path. | Same lifecycle recovery slice as above. | Same as above, plus schema/recovery regression tests. |
| P1 | #506 | Kill-switch/repo-disable paths end deployments without emitting idempotent terminal notifications. | True gap | [webhook-intent-worker.ts](/Users/neonwatty/Desktop/issuectl/packages/web/lib/webhook-intent-worker.ts:304), [webhook-pr-intent.ts](/Users/neonwatty/Desktop/issuectl/packages/web/lib/webhook-pr-intent.ts:161), [repos.ts](/Users/neonwatty/Desktop/issuectl/packages/web/lib/actions/repos.ts:659), and API route equivalent call `endDeployment`; [completion.ts](/Users/neonwatty/Desktop/issuectl/packages/web/lib/agent/completion.ts:77) shows notify call exists only on completion check-in. | Create a shared terminal-transition helper that ends deployment, updates PR review state when applicable, emits diagnostics, and calls `notifyDeploymentTerminalOutcome` once. Use it in webhook worker, PR worker, repo actions, API, and CLI where safe. | `pnpm --dir packages/web test -- webhook-intent-worker webhook-pr-intent actions/repos notifications`; `pnpm --dir packages/cli test -- repo` |
| P1 | #506/#507 | Raw webhook payload can render directly in dashboard log when raw mode retained it; redaction/minimization invariant is not enforced in UI. | True gap | [page.tsx](/Users/neonwatty/Desktop/issuectl/packages/web/app/logs/webhooks/page.tsx:294) renders `formatPayload(entry.payloadJson)`; [github-webhook-handler.ts](/Users/neonwatty/Desktop/issuectl/packages/web/lib/github-webhook-handler.ts:159) stores raw payload in raw mode. | Add payload redaction helper for webhook log display, mask common secret/token/body/comment fields, show redacted preview by default, and make any full raw export an explicit operator action if retained. | `pnpm --dir packages/web test -- webhook-redaction logs/webhooks`; `pnpm --dir packages/web typecheck` |
| P1 | #506/#507 | PR review timestamps appear stored as milliseconds but rendered as Unix seconds. | True gap | [webhook-pr-review-state.ts](/Users/neonwatty/Desktop/issuectl/packages/web/lib/webhook-pr-review-state.ts:63) writes `now` from worker `Date.now()`; [completion.ts](/Users/neonwatty/Desktop/issuectl/packages/web/lib/agent/completion.ts:177) writes `Date.now()`; [ReviewDetailPanel.tsx](/Users/neonwatty/Desktop/issuectl/packages/web/components/reviews/ReviewDetailPanel.tsx:219) multiplies by 1000. | Normalize PR review timestamps to milliseconds in UI helpers or migrate/store seconds consistently; add regression tests. | `pnpm --dir packages/web test -- review-detail-data SessionsReviewList`; `pnpm --dir packages/core test -- pr-reviews` |
| P1 | #506 | Default webhook PR action budgets allow child issue/PR creation, while #506 says child issues/PRs default to 0 for webhook sessions. | True gap | [launch-contexts.ts](/Users/neonwatty/Desktop/issuectl/packages/core/src/launch/launch-contexts.ts:105) seeds PR `create_issue = 1` and `create_pr = 1`. | Set child issue/PR defaults to 0 for webhook sessions, or split comment-command/manual override semantics if owner wants nonzero comment-command budget. | `pnpm --dir packages/core test -- launch-agent-command launch-execute-agents`; `pnpm --dir packages/web test -- agent/mutations mutation-actions` |
| P1 | #506 | Completion result shape is narrower than spec: only status, summary, final head SHA, and one pushed commit SHA are accepted. | True gap, lower than lifecycle/privacy | [completion.ts](/Users/neonwatty/Desktop/issuectl/packages/web/lib/agent/completion.ts:14) input type; [completion.ts](/Users/neonwatty/Desktop/issuectl/packages/web/lib/agent/completion.ts:144) result serialization. | Expand completion payload/schema handling for pushed commits array, changed file count, findings fixed/count, and error message while preserving backwards compatibility. | `pnpm --dir packages/web test -- agent/completion notifications`; `pnpm --dir packages/cli test -- agent` |
| P2 | #507 | Repo settings UX is simplified versus required header/status/health/per-label controls. | True UX gap | [RepoSettingsPanel.tsx](/Users/neonwatty/Desktop/issuectl/packages/web/components/repos/RepoSettingsPanel.tsx:194) header lacks GitHub link/status pill; [RepoSettingsPanel.tsx](/Users/neonwatty/Desktop/issuectl/packages/web/components/repos/RepoSettingsPanel.tsx:289) uses placeholder 7-day success text; [RepoSettingsPanel.tsx](/Users/neonwatty/Desktop/issuectl/packages/web/components/repos/RepoSettingsPanel.tsx:321) has one shared label recreation path. | Polish repo settings with real webhook health metrics, GitHub link/status, per-label health/recreate actions, and diagnostics for agent/preamble/payload changes. | `pnpm --dir packages/web test -- repos settings`; Playwright route smoke if UI changes are broad. |
| P2 | #507 | Sessions/reviews list is grouped but not fully threaded/collapsible, and filtering hides nonmatching siblings before grouping. | True UX gap | [SessionsReviewList.tsx](/Users/neonwatty/Desktop/issuectl/packages/web/components/sessions/SessionsReviewList.tsx:143) renders always-expanded groups; [sessions-data.ts](/Users/neonwatty/Desktop/issuectl/packages/web/lib/sessions-data.ts:178) filters before grouping; [sessions-data.ts](/Users/neonwatty/Desktop/issuectl/packages/web/lib/sessions-data.ts:309) trigger filter removes siblings. | Rework grouping/filtering so matching parents retain sibling context and expose the expected parent/child trigger affordances. | `pnpm --dir packages/web test -- sessions-data SessionsReviewList`; browser screenshot smoke. |
| P2 | #507 | Session detail triggered-by block and richer review-detail lineage are incomplete. | True UX gap | No session detail triggered-by block was found in app/component search; current review detail formats timestamps but lacks many specified expandable lineage fields. | Add session-detail provenance block and review-detail lineage metadata after lifecycle/privacy fixes. | `pnpm --dir packages/web test -- review-detail-data route-rendering`; Playwright route smoke. |
| P2 | #507 | Webhook log expansion/live-tail semantics are lighter than spec: invalid/replay rows separate, no signature/correlation/sibling chain/copy/download details, generic stream event. | True UX/operator gap | [page.tsx](/Users/neonwatty/Desktop/issuectl/packages/web/app/logs/webhooks/page.tsx:279) headers omit signature/correlation; [webhook-events-stream.ts](/Users/neonwatty/Desktop/issuectl/packages/web/lib/webhook-events-stream.ts:79) sends generic `webhook_events_changed`. | Fold audit rows into unified timeline or record approved deviation; add chain metadata and more specific stream events. | `pnpm --dir packages/web test -- webhook-events-stream logs/webhooks`; WebSocket proof if changed. |
| P2 | #506/#507 | Retention policy is hard-coded, not configurable as specified. | True gap | [github-webhook-handler.ts](/Users/neonwatty/Desktop/issuectl/packages/web/lib/github-webhook-handler.ts:40) raw payload 7-day constant; [webhook-payload-retention.ts](/Users/neonwatty/Desktop/issuectl/packages/web/lib/webhook-payload-retention.ts:7) event 30-day constant. | Add settings/config for raw payload and event retention; preserve long-lived delivery tombstones. | `pnpm --dir packages/core test -- webhooks settings`; `pnpm --dir packages/web test -- webhook-payload-retention` |
| P2 | #507 | API/web parity: `POST /api/v1/repos` accepts automation fields but does not install webhook/create labels/first-ping; `DELETE` does not best-effort delete GitHub webhook. | True API parity gap | [route.ts](/Users/neonwatty/Desktop/issuectl/packages/web/app/api/v1/repos/route.ts:107) only adds local repo/settings; [route.ts](/Users/neonwatty/Desktop/issuectl/packages/web/app/api/v1/repos/[owner]/[repo]/route.ts:47) removes local repo after ending sessions but no webhook delete call; [repos.ts](/Users/neonwatty/Desktop/issuectl/packages/web/lib/actions/repos.ts:342) web action does delete webhook best-effort. | Share repo install/remove service between Server Action and API route. | `pnpm --dir packages/web test -- api/v1/repos repos/actions`; `pnpm --dir packages/cli test -- repo` |
| P2 | #506 CLI additions | Some CLI names/options differ from #506 (`webhook rotate` vs `webhook secret rotate`, no `issue create --auto-launch`, no `pr create --auto-review`, no `repo add --webhook/--no-webhook`). | True gap unless owner accepts deviation | `packages/cli/src/index.ts` and `packages/cli/src/commands/webhook.ts` command surface; prior agent evidence. | Add aliases/options or record owner-approved CLI scope deviation. | `pnpm --dir packages/cli test -- repo webhook issue pr` |

## Already-Fixed Areas To Preserve

- Server-level webhook receiver, HMAC, replay, repo/hook binding, metadata/raw storage: [github-webhook-handler.ts](/Users/neonwatty/Desktop/issuectl/packages/web/lib/github-webhook-handler.ts:78).
- Generalized deployment targets, webhook intents, PR review table, action budgets: schema evidence in `packages/core/src/db/schema.ts`.
- Daemon-mediated mutation gateway and PR push safety gates: `packages/web/lib/agent/mutations.ts`.
- Comment command parser/permission/rate-limit/reaction substrate: `packages/web/lib/github-webhook-comment-command.ts`.
- Basic repo settings, webhook create/rotate/reinstall, label recreation, sessions/reviews/log routes: current web files.

## Acceptable Deviations Needing Owner/Judge Decision

- #506 says users supply tunnels; #507 asks onboarding to resolve/start `cloudflared` on demand. Current CLI warns and requires/proceeds with `public_webhook_base_url` ([repo.ts](/Users/neonwatty/Desktop/issuectl/packages/cli/src/commands/repo.ts:321)).
- #507 uses `/api/webhooks/github`; #506 and implementation use `/api/webhook/github/:repo_id`. Prefer #506 route unless owner wants alias.
- Comment-command sessions not killed by label removal appears consistent with #506’s proposed v1 default, but should be explicitly accepted.
- Some #507 details, such as exact icon glyphs and fully unified invalid/replay timeline, may be polish if operator diagnostics remain usable; Judge should classify before implementation.

## Recommended First Worker Slice

The first Worker should combine the highest-risk lifecycle issues rather than the raw-payload UI slice alone:

**Objective:** make terminal-ending and PR review lifecycle transitions recovery-safe and notification-consistent for webhook/comment-command sessions.

**Why:** This addresses P0/P1 correctness and notification invariants from #506, has clear evidence, and creates safer ground for later UI/API cleanup. It should cover PR review orphan recovery, PR launch linking, kill-switch/repo-disable notification emission, and tests around these transitions.

**Likely allowed files:** `packages/core/src/db/pr-reviews.ts`, `packages/core/src/db/pr-reviews.test.ts`, `packages/web/lib/webhook-pr-intent.ts`, `packages/web/lib/webhook-intent-worker.ts`, `packages/web/lib/actions/repos.ts`, `packages/web/lib/actions/repos.test.ts`, `packages/web/app/api/v1/repos/[owner]/[repo]/route.ts`, `packages/web/lib/push/notifications.ts` tests/mocks as needed, `packages/cli/src/commands/repo.ts`, and targeted tests.

**Verification:** `pnpm --dir packages/core test -- pr-reviews deployments`; `pnpm --dir packages/web test -- webhook-pr-intent webhook-intent-worker actions/repos notifications`; `pnpm --dir packages/cli test -- repo`.

## Final Audit Addendum

Status after T003-T010:

- Fixed: PR review launch/recovery safety, orphaned PR review recovery, terminal notification consistency, raw payload redaction, configurable webhook retention, PR review timestamp rendering, webhook PR child issue/PR budgets, richer completion result metadata, repo/operator UX detail, sessions/reviews sibling-preserving grouping and provenance, webhook stream payload stripping, API repo add/remove parity, `repo add --webhook/--no-webhook`, and `webhook secret rotate`.
- Owner-approved deviations/deferrals: use Cloudflare outside the app while retaining issuectl's operator-supplied public webhook URL model instead of managed `cloudflared` startup; keep `/api/webhook/github/:repoId` as the canonical receiver instead of adding a bare `/api/webhooks/github` alias; defer new CLI command modules for `issue create --auto-launch` and `pr create --auto-review`; and keep label removal scoped to label/webhook-created sessions while preserving explicit comment-command sessions.
- Local proof added after final audit rejection: `webhook_events_snapshot_updated` replaces the generic live-tail update event, and stream entries continue to omit raw payload JSON.
- Consolidated verification run after T010: `pnpm --dir packages/web test -- github-webhook-handler webhook-events-stream`; `pnpm --dir packages/web typecheck`; `pnpm --dir packages/core typecheck`; `pnpm --dir packages/cli typecheck`.
