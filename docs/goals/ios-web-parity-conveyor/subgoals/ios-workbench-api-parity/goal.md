# iOS Workbench API Parity Goal

## Objective

Add native Apple models and an API client refresh path for `/api/v1/workbench`, then use it to support the same all-repo board/session summary direction as the web workbench.

## Completion Proof

Merged PR with decoding tests, API client tests, focused iOS/macOS integration updates, local verification, adversarial review, and green CI.
